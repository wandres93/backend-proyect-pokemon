<h1>Lista de Pokémon (Modo Estático)</h1>

<div class="row">
  {{#each products}}
    <div class="col-md-4 mb-3">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">{{this.title}}</h5>
          <p class="card-text">{{this.description}}</p>
          <p class="text-primary fw-bold">${{this.price}}</p>
        </div>
      </div>
    </div>
  {{/each}}
</div>